-- 0001_initial.sql — initial AgentOS Studio schema.
-- Mirrors docs/supabase-setup.sql; intended to be applied via `pnpm migrate`
-- or piped to psql against a local Postgres + pgvector instance.

\i 'docs/supabase-setup.sql'
